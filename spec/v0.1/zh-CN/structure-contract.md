# Structure Contract v0.1

> 本文档为翻译版本。如与日文规范存在差异，以日文规范为准。

每个章节必须包含稳定的`section_id`、标题、`source_id`、一个或多个源页以及Web path。实现者应说明用于唯一定位artifact页面的页码规则。

一个Web章节可以跨多个源页。不得强制“一个PDF页对应一个Web页”。表格和插图可以包含ID与源页。

参见[section schema](../../../schema/v0.1/section.schema.json)。
